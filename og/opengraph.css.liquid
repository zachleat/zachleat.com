---
permalink: "static/opengraph.css"
---
* {
	margin: 0;
	padding: 0;
	box-sizing: border-box;
}
html {
	scrollbar-gutter: auto;
	/* The builder UI sits below the card, so the page must scroll — but a
	   scrollbar would eat pixels off the right edge of a 1200px screenshot. */
	scrollbar-width: none;
}
html::-webkit-scrollbar {
	display: none;
}
html,
body {
	margin: 0;
}
@font-face {
	font-family: SFDisplayBlack;
	src: url(/og/sources/sf-pro-display-black-latin.woff2) format("woff2");
}

:root {
	--slide-border-width: 3px;
	--og-w: 1200px;
	--og-h: 630px;
}

/* The screenshot target: sized exactly and pinned to the top left of the page
   so a 1200×630 viewport screenshot captures it edge to edge. */
.slide {
	container-type: inline-size;
	width: var(--og-w);
	height: var(--og-h);
	overflow: hidden;
	outline: none;
	border: var(--slide-border-width) solid transparent;
}
.slide,
.slide :link,
.slide :visited {
	color: #fff;
}

.slide:not(.slide-preview) {
	font-size: 1.25em; /* 20px /16 */
}
.slide:not(.slide-preview):not(.slide-editor),
.slide:not(.slide-preview):not(.slide-editor) code[class*="language-"],
.slide:not(.slide-preview):not(.slide-editor) pre[class*="language-"] {
	background-color: #000;
}
.slide-preview-vertical,
.slide-preview-vertical-flip,
.slide-preview-horizontal,
.slide-preview-horizontal-flip {
	display: flex;
}
.slide-preview-vertical {
	flex-direction: column-reverse;
}
.slide-preview-vertical-flip {
	flex-direction: column;
}
.slide-preview-vertical > pre,
.slide-preview-vertical-flip > pre {
	/* flex-grow: 1; */
	max-height: 58vh;
	overflow: auto;
}
.slide-preview-horizontal {
	flex-direction: row-reverse;
}
.slide-preview-horizontal-flip {
	flex-direction: row;
}
.slide-preview-horizontal .frame,
.slide-preview-horizontal-flip .frame {
	max-width: 60%;
}
.slide-preview-vertical .frame-resizer.frame-resizer,
.slide-preview-vertical-flip .frame-resizer.frame-resizer {
	flex-grow: 0;
	width: 70%;
	min-height: 30vh;
	resize: both;
}
.slide-preview-fullscreen .frame-resizer.frame-resizer {
	width: 90vw;
}

/* Ratios */
.slide-preview-30-70 pre {
	flex-basis: 30%;
}
.slide-preview-30-70 .frame {
	max-width: 100%;
}
.slide-preview-40-60 pre {
	flex-basis: 40%;
}
.slide-preview-50-50 pre {
	flex-basis: 50%;
}
.slide-preview-60-40 pre {
	flex-basis: 60%;
}
.slide-preview-70-30 pre {
	flex-basis: 70%;
}

/* Preview window */
.frame {
	display: flex;
	flex-grow: 1;
	justify-content: center;
	align-items: stretch;
	padding: 1em;
}
.frame-resizer-iframe {
	flex-grow: 999;
	border: none;
	width: 100%;
	height: 100%;
}

/* Fake window */
.window {
	display: flex;
	flex-direction: column;
	overflow: hidden;
	box-shadow: 0 10px 40px 4px rgba(0, 0, 0, .4);
	flex-grow: 999;
}
.window-hed {
	display: flex;
	background-color: #222;
	gap: 6px;
	padding: 8px;
}
.window-circle {
	display: inline-block;
	height: 14px;
	min-width: 14px;
	max-width: 14px;
	border-radius: 14px;
}
.window-circle-red {
	background-color: #FF5F56;
}
.window-circle-yellow {
	background-color: #FFBD2E;
}
.window-circle-green {
	background-color: #27C93F;
}
.window-spacer {
	flex-grow: 999;
}
.window-title {
	text-align: center;
	font-size: 0.75em; /* 12px /16 */
	overflow: hidden;
	white-space: nowrap;
	text-overflow: ellipsis;
	padding-left: 1.5em;
	padding-right: 1.5em;
	display: flex;
	align-items: center;
	justify-content: center;
	margin: -4px 0 -6px;
	text-decoration: none;
	font-family: -apple-system, system-ui, sans-serif;
	font-weight: 600;
}
.window-title.window-title.window-title,
.window-title.window-title.window-title:visited {
	color: #fff;
}

/* Main content */
.main {
	--main-bg-overlay: rgba(255,255,255,.15);
	position: relative;
	color: #fff;
	height: 100%;
	display: flex;
	flex-direction: column;
	justify-content: flex-start;
	align-items: flex-start;
	font-size: 2em;
	padding-top: 1em;
}
.main > * {
	z-index: 1;
}
.main > .main-top {
	z-index: 2;
}
.main:before {
	content: "";
	position: absolute;
	left: 0;
	right: 0;
	top: 0;
	bottom: 0;
	background-image: var(--og-bgimage, linear-gradient(238deg, #a9c7a0, #96b58d, #7aa16f, #466e3b, #496d41, #3d5a3d, #203b1d, #162a15));
	background-size: 100% auto;
	background-position: center;
}
.main:after {
	content: "";
	background-color: var(--main-bg-overlay);
	position: absolute;
	left: 0;
	right: 0;
	top: 0;
	bottom: 0;
}
.main-bgimg {
	{% comment %} --main-bg-overlay: rgba(0,0,0,.4); {% endcomment %}
	{% comment %} --main-bg-overlay: rgba(255,255,255,.3); {% endcomment %}
	{% comment %} --main-bg-overlay: hsla(107deg, 23%, 70%, .3); {% endcomment %}
	--main-bg-overlay: hsla(117deg, 24%, 30%, .4);
}

.main-top {
	padding-left: 1.5em;
	padding-right: 1.5em;
}

.main-hed {
	display: -webkit-box;
	-webkit-box-orient: vertical;
	-webkit-line-clamp: 4;
	font-size: 2.5em;
	font-variation-settings: "wdth" 100, "TYTE" -30, "NOTC" 1;
	overflow: hidden;
	line-height: 1.3;
	padding: 0;
	width: 100%;
}
.main-hed,
.main-hed code {
	color: #fff;
}
.main-hed.main-hed {
	margin: 0;
}
.main-hed-long {
	font-size: clamp(2.2em, 4.5cqw, 3.5em)
}
.main-hed-sup {
	line-height: 1.5;
	font-family: Lato;
	font-weight: 400;
	font-size: 1.5rem;
	vertical-align: top;
	background-color: rgba(0,0,0,.3);
	text-transform: none;
	letter-spacing: 0;
}
@supports (box-decoration-break: clone) or (-webkit-box-decoration-break: clone) {
	.main-box {
		text-wrap: balance;
	}
	.main-box {
		display: inline;
		padding: 0 .2em;
		-webkit-box-decoration-break: clone;
		box-decoration-break: clone;
		background-color: rgba(0,0,0,.9);
	}
	.main-box-nowrap {
		white-space: nowrap;
	}
}
.main-left[hidden] {
	display: none;
}
.main-left {
	position: absolute;
	bottom: 0;
	left: 0;
	text-align: left;
	display: flex;
	align-items: center;
}
.main-left .main-avatar-img-big {
	margin-left: -.5em;
	margin-bottom: 0;
	width: 12em;
}
.main-center {
	position: absolute;
	left: 50%;
	bottom: 1em;
	transform: translateX(-50%);
}
.main-right {
	position: absolute;
	font-size: 0.875em; /* 14px /16 */
	bottom: 1em;
	right: 1em;
	text-align: right;
}
.main-right > * {
	line-height: 1.5;
}
/* .main-box sets `display: inline`, which outranks the UA rule for [hidden] */
.main-right [hidden] {
	display: none;
}

.main-host {
	margin-right: 1em;
}
.main-postnumber {
	margin-right: 1em;
}

.slide-chromeless {
	border: none;
}
.slide-chromeless .frame {
	padding: 0;
}
.slide-chromeless .window {
	box-shadow: none;
}
/* Builder UI, deliberately below the card so it stays out of screenshots */
.ui {
	max-width: var(--og-w);
	padding: 2rem 2.5rem 5rem;
	font-family: -apple-system, system-ui, sans-serif;
	font-size: 1rem;
	line-height: 1.4;
	color: #222;
}
.ui h2 {
	margin: 0 0 .25em;
}
.ui p {
	margin: 0 0 1.5em;
	color: #666;
}
.ui-fields {
	display: grid;
	gap: 1rem;
}
.ui label {
	display: grid;
	gap: .35em;
	align-content: start;
	font-weight: 600;
}
.ui select {
	font: inherit;
	font-weight: 400;
	padding: .5em .65em;
	border: 2px solid #ccc;
	border-radius: .4em;
	background-color: #fff;
	color: inherit;
	min-width: 0;
}
.ui-actions {
	margin-block-start: 1.5rem;
	display: flex;
	flex-wrap: wrap;
	gap: .75rem;
	align-items: center;
}
.ui-actions a,
.ui-actions button {
	font: inherit;
	font-weight: 600;
	cursor: pointer;
	padding: .55em 1.2em;
	border: 2px solid #203b1d;
	border-radius: 100px;
	background-color: #203b1d;
	line-height: 1;
	text-decoration: none;
}
.ui-actions a,
.ui-actions a:visited,
.ui-actions button {
	color: #fff;
}
.ui code {
	font-family: ui-monospace, Menlo, Consolas, monospace;
	background-color: #eee;
	padding: .15em .4em;
	border-radius: .3em;
}
